name: CI/CD Pipeline FrontEnd App

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Logging
        run: echo "Frontend Deployed Successfully"
